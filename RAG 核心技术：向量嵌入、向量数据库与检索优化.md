# RAG 核心技术：向量嵌入、向量数据库与检索优化

## 1. 向量嵌入：原理与实践要点

### 1.1 核心原理
向量嵌入（Embedding）是将文本/图像/音频等非结构化数据，通过嵌入模型转换为高维向量表示，以捕捉语义或特征关联。检索时通过“向量相似度/距离”衡量相关性。

常用相似度/距离度量：
- **余弦相似度（Cosine）**：关注向量方向一致性，不受长度影响，适合文本语义匹配。
- **欧氏距离（L2）**：度量几何距离，适合连续数值特征（如图像像素等）。
- **内积（IP）**：当向量长度有意义时使用；向量归一化后与余弦相似度等价，推荐/排序场景常用。

### 1.2 嵌入技术分类（与检索相关）
- **密集嵌入（Dense Embedding）**
  - 特征：连续实数、维度相对较小（约 128–4096），语义表达强
  - 典型：BGE-M3、text-embedding-3-large
  - 场景：语义检索、跨模态匹配
- **稀疏嵌入（Sparse Embedding）**
  - 特征：高维、绝大多数为 0，位置常对应显式特征（词项等）
  - 典型：BM25、SPLADE
  - 场景：关键词精确匹配、全文检索

---

## 2. 向量数据库：特性与使用

### 2.1 核心概念与架构要素
- **Database**：逻辑隔离单元（多租户/环境隔离）
- **Collection**：类似关系型数据库的“表”，保存向量字段与标量字段
- **Entity**：一条记录（主键 + 向量 + 元数据/标量字段）
- **Index**：加速向量检索；通常需要指定向量字段与度量类型（Cosine/L2/IP）

### 2.2 主流向量数据库对比（选型视角）
- **Milvus（开源）**
  - 特点：多索引、可横向扩展、支持混合检索
  - 场景：大规模生产、高并发
  - 建议：数据量 ≥ 1 亿，对吞吐/可用性要求高
- **Weaviate（开源，可托管）**
  - 特点：多模态 Any-to-Any、GraphQL 接口，上手快
  - 场景：PoC、MVP 快速上线
  - 建议：希望“开箱即用”、运维成本低
- **Qdrant（开源）**
  - 特点：过滤能力强、低延迟、Rust 内核
  - 场景：语义搜索 + 复杂过滤
  - 建议：中小规模集群，兼顾召回与过滤
- **Pinecone（托管，非开源）**
  - 特点：全托管/Serverless、自动扩缩容、混合检索、多租户
  - 场景：快速上线大规模服务
  - 建议：项目早期/运维人力少
- **Chroma（开源）**
  - 特点：本地/轻量，Python 友好，零运维
  - 场景：原型、本地测试
  - 建议：数据量 < 10 万
- **Elasticsearch（商业/托管生态，支持向量）**
  - 特点：全文检索 + 向量混合，生态成熟
  - 场景：已有 ES 体系，增量引入向量搜索
  - 建议：优先复用现有 ES 集群能力
- **PGVector（开源，PostgreSQL 插件）**
  - 特点：关系数据 + 向量混合查询
  - 场景：已有 PG，并且只需“够用”的向量检索
  - 建议：数据量 < 千万级、混合数据查询需求强

### 2.3 常见索引类型与适用场景
- **FLAT（暴力全量）**
  - 优点：100% 召回
  - 缺点：慢、内存压力大
  - 场景：小数据（<10 万）且追求高精度
- **IVF_FLAT（聚类分桶 + 桶内暴力）**
  - 优点：速度快、召回较高
  - 缺点：聚类参数影响精度
  - 场景：中大规模，平衡速度/精度
- **IVF_PQ（IVF + 量化压缩）**
  - 优点：内存低、查询快
  - 缺点：召回略损
  - 场景：内存紧张、可接受精度折中
- **HNSW（分层图）**
  - 优点：低延迟、高召回
  - 缺点：构建成本高
  - 场景：Top-K 小、低延迟诉求强
- **DISKANN（磁盘 + 图遍历 + 量化）**
  - 优点：支持超大规模
  - 缺点：依赖 SSD IOPS
  - 场景：数据超内存、对时延有一定容忍

### 2.4 使用要点（工程侧）
- **分块与分区**：大文档做语义分块；按业务维度（时间/类别等）分区提升效率
- **一致性与备份**：按业务选择一致性策略；开启备份与灾备机制

---

## 3. 提升检索效率与效果的技术（RAG 检索优化）

### 3.1 检索前处理（Query 侧优化）
- **查询重写**：去噪、术语标准化、明确意图
- **查询分解**：复杂问题拆子问题分别检索
- **查询澄清**：对模糊问题先问澄清问题再检索
- **查询路由**
  - 逻辑路由：按业务类别选数据源
  - 语义路由：按查询向量选择不同嵌入/提示词/检索策略
- **HyDE 扩展**：先生成“假设性文档”，再用该文档向量去检索，提高零样本检索效果

### 3.2 索引与数据组织优化（Index 侧）
- **层次索引**：摘要索引 → 细节索引，先粗定位再细检索
- **多表示索引**：同一条数据生成多类向量（语义向量/关键词向量等）
- **父子文档索引**：片段与原文档关联；先检索片段再聚合到文档级，兼顾相关性与多样性
- **动态索引策略**：按更新频率/数据特征选择不同索引方案（增量 vs 静态）

### 3.3 检索策略优化（Search 侧）
- **混合检索**
  - 稀疏 + 密集：BM25（关键词） + Dense（语义）
  - 多模态：文本向量 + 图像向量，支持图文互搜
- **过滤与范围检索**
  - 先标量过滤（时间、类别等）再向量检索
  - 使用相似度区间（radius/range）筛选结果
- **分组检索（Grouping）**
  - 按 docId 分组取每组 TopN，避免同一文档多个片段“霸榜”，提升多样性
- **重排序（Reranking）**
  - WeightedRanker：按权重融合多路结果
  - RRFRanker：平衡融合，提升整体排序鲁棒性

### 3.4 嵌入侧优化（Embedding 侧）
- **混合嵌入**：稀疏（关键词）+ 密集（语义）互补
- **领域适配**：用垂直领域嵌入模型提升专业场景命中率
- **长文本处理**：滑动窗口分块、摘要嵌入，避免长文本语义稀释

---
